# Local development:
VITE_API_BASE_URL=http://127.0.0.1:8000

# If Nginx serves frontend and proxies /api to FastAPI on the same domain,
# use an empty value when building on the server:
# VITE_API_BASE_URL=
